# pubs-mysql
A fully compatible MySQL 8.0+ port of the classic Microsoft SQL Server “Pubs” sample database. This script recreates the entire Pubs database schema, data, views, stored procedures, and triggers using standard MySQL syntax.
